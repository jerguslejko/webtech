extends _layout

block content
    <h1>Hey, #{signedIn.username}</h1>

    <h2>Your Kanban Boards:</h2>

    <ul>
        for board in boards
            <li>
                <a href="/boards/#{board.id}">#{board.title}</a>
            </li>
    </ul>
